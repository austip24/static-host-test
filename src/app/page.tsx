import { Calendar } from "@/components/calendar";

const Homepage = () => {
	return (
		<main className="flex flex-col grow h-main-content">
			<div className="grow">
				Testing app host
				{/* <Calendar /> */}
			</div>
		</main>
	);
};

export default Homepage;
